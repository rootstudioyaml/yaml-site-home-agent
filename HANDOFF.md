# 핸드오프: 브라우저 작업 이어받기 (2026-09-06)

노트북의 대화형 세션에서 이어서 진행하기 위한 문서입니다. 이 저장소를 `git pull`
한 뒤 이 문서와 `LOCAL-SEO.md`를 함께 읽으면 상황을 파악할 수 있습니다.

## 왜 넘어왔는가

직전 세션은 비대화형으로 실행되어 권한 승인 창이 뜨지 않았고, 첫 브라우저 호출
단계에서 다음 오류로 막혔습니다.

```
Claude requested permissions to use mcp__playwright__browser_navigate,
but you haven't granted it yet.
```

브라우저 로그인 여부와 무관하게 권한 단계에서 멈춘 것이므로, 대화형 세션에서
승인만 하면 그대로 이어갈 수 있습니다.

## 이미 끝난 작업

커밋 `9b60a57`으로 origin main에 반영되어 배포까지 나갔습니다.

- 구조화 데이터의 주소에 `streetAddress`를 추가했습니다.
- 구조화 데이터의 `sameAs`와 `hasMap`에 네이버 플레이스 주소를 추가했습니다.
- 대표자 정보를 `founder` 항목으로 추가했습니다.
- 연락처 영역에 네이버 플레이스 링크를 노출했습니다.
- `sitemap.xml`의 `lastmod`를 2026-09-06으로 갱신했습니다.
- `LOCAL-SEO.md`에 작업 계획과 붙여넣을 문구를 모두 정리했습니다.

## 진단 결과 요약

2026-09-06에 네이버 모바일 검색 결과를 직접 확인해 얻은 내용입니다.

- 등록 실패가 아니라 동명 업체와의 경쟁에서 밀린 상황입니다.
- 검색어 "제이하우징" 최상단은 동명 법인 `제이하우징(주)`의 기업분석 모듈입니다.
- 플레이스에 같은 이름이 세 곳 있고, 우리 업체는 두 번째입니다.
  - 우리 업체: 플레이스 ID `2007821211` · 내장목공사 · 서울 송파구 마천동
  - 도봉구 창동: `1242120918`
  - 경기 파주시 적성면: `1885822291`
- 사업자등록증 상호가 `제이하우징` 단독이라 업체명으로는 차별화할 수 없습니다.
- 웹사이트는 이미 색인되어 있습니다.

## 이어서 할 일

사용자 지시에 따라 원래 계획의 3번 항목인 리뷰 요청은 **진행하지 않습니다.**
아래 세 가지만 브라우저로 처리합니다.

### 1. 구글 비즈니스 프로필 인증 상태 확인

`https://business.google.com/locations`에 접속해 제이하우징 항목의 상태가
"게시됨"인지 "인증 대기"인지 확인합니다. 인증이 대기 상태라면 요청된 방식으로
인증을 진행합니다. 여기가 막혀 있으면 구글 지도와 검색에 아예 노출되지 않으므로
가장 먼저 확인해야 합니다.

확인한 뒤에는 출장 시공업에 맞게 서비스 지역 업체로 설정하고, 서비스 지역에
서울 전역과 경기, 인천을 등록합니다. 소개 문구와 서비스 목록은 `LOCAL-SEO.md`
3절의 내용을 그대로 사용합니다.

### 2. 네이버 스마트플레이스 정보 입력

`https://new.smartplace.naver.com/`에 접속해 `LOCAL-SEO.md` 3절의 내용을
입력합니다. 업종 분류 `내장목공사`는 그대로 두고, 대표 키워드 12개와 업체 소개
문구를 넣은 뒤 NAP 표기를 표에 적힌 대로 맞춥니다.

사진은 `assets/` 디렉터리의 파일을 사용해 20장 이상 등록합니다. 사용할 파일
목록은 `LOCAL-SEO.md` 5절에 적어 두었습니다.

### 3. 검색엔진 등록

소유권 확인용 파일은 이미 배포되어 있으므로 확인 절차는 통과합니다.

- 구글 Search Console(`https://search.google.com/search-console`)에
  `j-housing.co.kr` 속성을 추가하고 `https://j-housing.co.kr/sitemap.xml`을
  제출한 뒤, URL 검사 도구로 색인을 요청합니다. 확인 파일은
  `googlec9a9497dafcf1366.html`입니다.
- 네이버 서치어드바이저(`https://searchadvisor.naver.com/`)에 사이트를 등록하고
  사이트맵을 제출합니다. 확인 파일은
  `naver2c82333c4b627ec603d51cc7e1cc002c.html`입니다.

## 미리 알아 둘 걸림돌

playwright MCP는 기본적으로 자체 프로필로 크롬을 띄우기 때문에, 평소 쓰는 크롬
프로필의 로그인 세션이 공유되지 않을 수 있습니다. 구글 계정 로그인 화면이 먼저
뜨고 2단계 인증이 필요해지면 자동화가 중간에 멈춥니다. 그 경우에는 로그인만
직접 처리한 뒤 이후 조작을 이어가면 됩니다.

## 작업을 마친 뒤

브라우저 작업의 결과로 웹사이트 파일을 고쳤다면 커밋해서 origin main에 푸시해야
배포가 나갑니다. 작업을 끝내기 전에 `git status`로 남은 변경이 없는지 확인합니다.

이 문서는 브라우저 작업이 모두 끝나면 삭제해도 됩니다.
